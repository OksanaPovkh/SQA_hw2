// 1.Вывести предпоследний заказ
SELECT *
FROM Orders
ORDER BY OrderID DESC
LIMIT 1 OFFSET 1;

//2. Вывести пять самых старых заказов
SELECT *
FROM Orders 
ORDER BY OrderDate ASC 
LIMIT 5;

//3.Вывести номера телефонов поставщиков, кроме тех, что из UK и Brazil
SELECT SupplierID, Phone 
FROM Suppliers
WHERE Country NOT IN ('UK', 'Brazil');

//4.Вывести товары из категорий 4 и 5 с ценой до 250 EUR (вкл.)
SELECT *
FROM Products
WHERE CategoryID IN (4, 5)
AND 
Price <= 250

//5.Дайте ответ на вопрос, зачем нужна проекция в БД
-Сократить количество возвращаемых данных, выбрав только необходимые столбцы. Это повышает эффективность, т.к. из БД извлекается и обрабатывается меньший объем данных.
-Скрыть лишнюю или конфиденциальную информацию.Проекция позволяет вернуть пользователю или приложению только те данные, которые необходимы для работы.
-Изменить представление данных. Проекция может менять порядок столбцов, их имена, вычислять выражения.
-Оптимизировать обработку данных в запросе за счет уменьшения объема оперируемых данных.